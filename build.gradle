apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.json:json:20140107'

    testCompile 'junit:junit:4.+'
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.11'
}
