apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile 'net.minidev:json-smart-mini:1.+'

    testCompile 'junit:junit:4.+'
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.11'
}
